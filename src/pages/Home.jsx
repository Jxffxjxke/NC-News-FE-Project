import React from "react";
import ArticlesList from "../components.jsx/ArticlesList";

const Home = () => {
  return (
    <main>
      <ArticlesList />
    </main>
  );
};

export default Home;
